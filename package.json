{
  "name": "app",
  "version": "0.0.1",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/server.js",
    "docker:build": "docker build -t app .",
    "docker:run": "docker run --rm -p 80:80 app"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
